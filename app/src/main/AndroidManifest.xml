<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.n.nomoko">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <service
            android:name=".logic.ChallengeService"
            android:process=":defis" />

        <activity android:name=".views.AccueilActivity" />
        <activity android:name=".views.InscriptionActivity" />
        <activity android:name=".views.RecompenseActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".views.LogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.TropheeActivity" />
        <activity android:name=".views.TestPersoActivity" />
        <activity android:name=".views.BienvenueActivity" />
        <activity android:name=".views.ResultatTestActivity"></activity>
    </application>

</manifest>